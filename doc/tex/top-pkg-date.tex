

The standard library {\tt date} package implements  
access to the Posix {\tt strftime()} function and related functionality.

The {\tt date} package implements the \ahrefloc{api:Date}{Date} API.

The {\tt date} package source code is in \ahrefloc{src/lib/std/src/date.pkg}{src/lib/std/src/date.pkg}.

