
The standard library {\tt iterate} package implements the {\tt iterate} {\tt repeat} and {\tt forloop} iterator functions.

The {\tt iterate} package implements the \ahrefloc{api:Iterate}{Iterate} API.

The {\tt iterate} package source code is in \ahrefloc{src/lib/src/iterate.pkg}{src/lib/src/iterate.pkg}.
