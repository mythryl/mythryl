#!/usr/bin/mythryl
#
# Trivial demo of using a library from a script: 

load "factor.lib";

{
    fun print_int i =  printf "%d " i;
    factors = factor::factors(24);
    apply print_int factors;
};
