## benchmark.api

# Compiled by:
#     src/app/benchmarks/benchmarks.lib

stipulate
    package bj  =  benchmark_junk;				# benchmark_junk	is from   src/app/benchmarks/benchmark-junk.pkg
herein

    api Benchmark {
	#
	run_benchmark: Int -> bj::Benchmark_Result;		# We always pass in an argument, to ensure the benchmark isn't a constant expression.
    };
end;