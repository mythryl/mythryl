SUBLIBRARY_EXPORTS

SUBLIBRARY_COMPONENTS

	 #if defined(X_Y)
	    #error X_Y defined !
	 #endif
	a.pkg
	 #if not defined (package a)
	    #error No definition for package a!
	 #endif
	b.pkg : sml
	c.pkg
	 #if (3 > 2)
.pkg
	 #elif not (4 > 5)
       .pkg
	 #else
       .pkg
	 #endif
