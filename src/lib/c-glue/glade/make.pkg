fun make file = if cm::make file then () else raise exception FAIL("Can't load "$file);
fun again () = run "make.pkg";

make "../ffi-packager/ffi-packager.cm";

/*---------------------------------------------------------------------------
 * 
 * This program drives the sml-glade library packaging process
 *
 *---------------------------------------------------------------------------*/
package p =
gift_wrap_g
(  use ffi_packager
   use gift_wrap_defaults
   package a = Ast
  
   name     = "glade"
   name'    = "glade"
   version  = "0.1"
   enums    = [("glade-enums.sml", "GladeEnums", "")]
   defines  = /* [("glade-macros.sml", "GladeMacros",
                     [
                      ("^[A-Z]", FALSE)
                     ]
                   )
                  ] */ []
   fun init info = 
       (tr::declare info "^f-" (FastString o BoolIfCtypeIs "bool")
       )
)
