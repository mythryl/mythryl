/*
 * GC Code motion
 */
generic package GCCodeMotion
   (package ir:  LOWHALF_IR
    package gc:  Per_Codetemp_Heapcleaner_Info_Template
   ) : GC_CODE_MOTION
{

   package ir  = IR
   package machcode_controlflow_graph = ir::machcode_controlflow_graph
   package gc  = GC
   package g   = graph

   fun gcSafeCodeMotion (IR as g::GRAPH mcg) =
       IR

}
