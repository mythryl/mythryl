/*
 * GC Code motion
 */
generic package GCCodeMotion
   (package ir:  LOWCODE_IR
    package gc:  Garbage_Collector_Type
   ) : GC_CODE_MOTION
{

   package ir  = IR
   package control_flow_graph = ir::control_flow_graph
   package gc  = GC
   package g   = graph

   fun gcSafeCodeMotion (IR as g::GRAPH cfg) =
       IR

}
