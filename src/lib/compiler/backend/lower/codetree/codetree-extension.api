## codetree-extension.api

# Compiled by:
#     src/lib/compiler/backend/lower/make7/lowcode.make6

api Codetree_Extension {

    Sx(  S, R, F, C);		# "Sx" == "statement extension"
    Rx(  S, R, F, C);		# "Rx" == "(integer) register extension"
    Fx(  S, R, F, C);		# "Fx" == "float-register extension"
    Ccx( S, R, F, C);		# "Ccx" == "condition code (==boolean) extension"
};
