(* script to load the trace menu widget code *)

(* load the buffered channels from the MULTIPROG library *)
(* use "/usr/local/sml/multiprog-0.9.8/library/buffer.pkg"; *)
use "/home/jhr/sml/MULTIPROG/library/buffer.pkg";

structure BufferChan = BufferChan (MULTIPROG);

app use [
    "trace-menu.api",
    "trace-menu.pkg"
  ];

