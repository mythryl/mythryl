Global-scale GUI things:

  Gui_Tree,  the datastructure specifying a GUI (other than the Theme stuff).

  gui_queen_imp, the package managing GUI startup, shutdown and operation,
             in distribution of events to widgets.
