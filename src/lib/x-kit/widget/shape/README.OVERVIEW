Widget = (State, Shape, Skin) where
    State contains core state information,
    Shape handles layout geometry and
    Skin  handles putting pixels on the screen.

Here we handle the Shape components, which are imps in their own right.
