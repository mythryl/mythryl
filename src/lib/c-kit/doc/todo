1. document key signatures and other files

  parser
+   parse-tree.api  (parse tree representation)
+   parser-sig.pkg

  parser/util
+   error.api
+   sourcemap.api

  ast/pp  (pretty printing ast)
    pp-ast.api

  ast
    ast.api        (abstract syntax representation)
+   parse-to-ast.api
    (tables: tid, pid, aid, corresponding tables)

  src
    Makefile.make7


2. config

  variants  (configuration)
    config-sig.pkg
    sizes.api
    parse-control.api
    type-check-control.api


3. extensions

  parse/extensions/x/   (x = c, ...)
    parse-tree-ext.api

  ast/extensions/x/
    ast-ext.api

  ast/
    cnv-ext.api

  lexing & parsing
    c.lex
    c.grammar
    token table
    build-ast.pkg
    size ...


--------------------------- Wed Oct 27 15:25:42 1999 --------------------------

Document the regression test procedure....
