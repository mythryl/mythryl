This subtree contains the stuff that didn't
find a better home anywhere else.

src/etc/pathconfig
    This file contains the default anchor bindings
    used in .lib files, currently just:
        $ROOT

