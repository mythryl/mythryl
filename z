#!/usr/bin/mythryl

foo = REF 0;

fun bar ()
    =
    {   sleep 0.01;
	#
	foo := 1;
	#
	pthread::pthread_exit ();
    };

pthread = pthread::spawn_pthread bar;
pthread::join_pthread pthread;

printf "*foo d=%d\n" (*foo);



